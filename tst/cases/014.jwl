// @ts-nocheck

import { exit } from './helpers';

let a = [1, 2, 3] as vec3;
let b = [4, 5, 6] as vec3;
let c = add(a, b);

exit(c.y);

function add(a: vec3, b: vec3): vec3 {
    return [a.x + b.x, a.y + b.y, a.z + b.z];
}

type vec3 = {
    x: s32,
    y: s32,
    z: s32,
};