// @ts-nocheck

import { exit } from './process';

exit(add([1, 2, 3], [4, 5, 6]).z);

function add(a: vec3, b: vec3): vec3 {
    return [a.x + b.x, a.y + b.y, a.z + b.z];
}

type vec3 = {x: s32, y: s32, z: s32};